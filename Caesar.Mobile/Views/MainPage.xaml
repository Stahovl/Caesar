<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Caesar.Mobile.Views.MainPage"
             Title="Caesar Admin">
    <StackLayout>
        <Button Text="Manage Menu Items"
                Command="{Binding NavigateToMenuItemsCommand}" />
        <Button Text="Manage Reservations"
                Command="{Binding NavigateToReservationsCommand}" />
        <!-- Add more buttons for other admin functions -->
    </StackLayout>
</ContentPage>